---
 - name: this is main playbook from here all roles will be called
   hosts: localhost
   connection: local
   remote_user: rohan
   vars:
     ansible_python_interpreter: "/home/rohan/multitierphpproject/myenv/bin/python3"
   vars_files:
     - vault.yaml

 - name: creating an iam role...
   hosts: localhost
   remote_user: ubuntu
   roles:
     - role: iamrolecreationall

